apiVersion: v1
kind: Service
metadata:
  name: react-app
  namespace: react-app
  labels:
    app: react-app
spec:
  type: ClusterIP
  selector:
    app: react-app
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
