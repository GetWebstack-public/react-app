apiVersion: v1
kind: Service
metadata:
  name: gws-playground-front
  labels:
    app: gws-playground-front
spec:
  type: ClusterIP
  selector:
    app: gws-playground-front
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
